export declare class Cache {
    private compilerCachePath;
    constructor();
    list(): string[];
    add(code: string, fileName: string): void;
    has(fileName: string): boolean;
    resolve(fileName: string): string;
}
